
==================== Selector: \.character-overlay (Found 12) ====================
Occurrence 1:
ffecd2, #fcb69f); }
.character-avatar-6 { background: linear-gradient(135deg, #4facfe, #00f2fe); }

.character-overlay {
    position: absolute;
    bottom: 10px;
    right: 10px;
    opacity: 1;
    pointer-events: all;
    transition: all 0.3s ease;
}

/* Небольшое увеличение при наведении */
.character-card:hover .character-overlay .btn-select {
    transform: scale(1.05);
}

.program-card:hover .character-overlay .btn-select {
    transform: none;
}

.character-info {
    padding: 1.5rem;
}

.character-header
----------------------------------------
Occurrence 2:
l;
    transition: all 0.3s ease;
}

/* Небольшое увеличение при наведении */
.character-card:hover .character-overlay .btn-select {
    transform: scale(1.05);
}

.program-card:hover .character-overlay .btn-select {
    transform: none;
}

.character-info {
    padding: 1.5rem;
}

.character-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    margin-bottom: 0.5rem;
}

.btn-order-inline {
    opacity: 0;
    transform: translateX(20px);
    transition: all 0
----------------------------------------
Occurrence 3:
cter-card:hover .character-overlay .btn-select {
    transform: scale(1.05);
}

.program-card:hover .character-overlay .btn-select {
    transform: none;
}

.character-info {
    padding: 1.5rem;
}

.character-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    margin-bottom: 0.5rem;
}

.btn-order-inline {
    opacity: 0;
    transform: translateX(20px);
    transition: all 0.3s ease;
    pointer-events: none;
    padding: 0.5rem 1rem;
    font-size: 0.9rem;
----------------------------------------
Occurrence 4:
tant;
        text-overflow: ellipsis !important;
    }

    /* Selection overlay and button */
    .character-overlay {
        bottom: 6px !important;
        right: 6px !important;
        left: 6px !important;
    }

    .character-overlay .btn-select {
        width: 100% !important;
        padding: 0.35rem 0.5rem !important;
        font-size: 0.75rem !important;
        justify-content: center !important;
        border-radius: 10px !important;
        min-height: auto !important;
        height: auto !im
----------------------------------------
Occurrence 5:
    bottom: 6px !important;
        right: 6px !important;
        left: 6px !important;
    }

    .character-overlay .btn-select {
        width: 100% !important;
        padding: 0.35rem 0.5rem !important;
        font-size: 0.75rem !important;
        justify-content: center !important;
        border-radius: 10px !important;
        min-height: auto !important;
        height: auto !important;
    }

    .character-overlay .btn-select-hours {
        width: 100% !important;
        padding: 0.25rem 0.5rem !i
----------------------------------------
Occurrence 6:
: 10px !important;
        min-height: auto !important;
        height: auto !important;
    }

    .character-overlay .btn-select-hours {
        width: 100% !important;
        padding: 0.25rem 0.5rem !important;
        font-size: 0.75rem !important;
        justify-content: space-between !important;
        border-radius: 10px !important;
        height: auto !important;
        min-height: auto !important;
    }

    .character-overlay .btn-select-hours button {
        width: 20px !important;
        height
----------------------------------------
Occurrence 7:
: 10px !important;
        height: auto !important;
        min-height: auto !important;
    }

    .character-overlay .btn-select-hours button {
        width: 20px !important;
        height: 20px !important;
        font-size: 0.8rem !important;
        line-height: 18px !important;
    }

    .character-overlay .hours-value {
        font-size: 0.75rem !important;
    }

    /* Image Badges and counters */
    .character-photo-counter {
        top: 6px !important;
        right: 6px !important;
        paddi
----------------------------------------
Occurrence 8:
 !important;
        font-size: 0.8rem !important;
        line-height: 18px !important;
    }

    .character-overlay .hours-value {
        font-size: 0.75rem !important;
    }

    /* Image Badges and counters */
    .character-photo-counter {
        top: 6px !important;
        right: 6px !important;
        padding: 0.15rem 0.4rem !important;
        font-size: 0.7rem !important;
        border-radius: 8px !important;
    }

    .character-badges {
        top: 6px !important;
        left: 6px !important;

----------------------------------------
Occurrence 9:
 }

/* Overrides for overlay controls inside additional services to match program hours selector */
.character-overlay .additional-service-qty {
    background: rgba(255, 255, 255, 0.18);
}

.character-overlay .additional-service-qty-value {
    color: white;
}

.character-overlay .additional-service-qty-btn {
    border-color: rgba(255, 255, 255, 0.6);
    background: rgba(255, 255, 255, 0.15);
    color: white;
}

.character-overlay .additional-service-qty-btn:hover {
    background: rgba(255, 255, 255, 0.25);

----------------------------------------
Occurrence 10:
ector */
.character-overlay .additional-service-qty {
    background: rgba(255, 255, 255, 0.18);
}

.character-overlay .additional-service-qty-value {
    color: white;
}

.character-overlay .additional-service-qty-btn {
    border-color: rgba(255, 255, 255, 0.6);
    background: rgba(255, 255, 255, 0.15);
    color: white;
}

.character-overlay .additional-service-qty-btn:hover {
    background: rgba(255, 255, 255, 0.25);
    border-color: white;
}

.additional-service-badge {
    position: absolute;
    top: 16
----------------------------------------
Occurrence 11:
ba(255, 255, 255, 0.18);
}

.character-overlay .additional-service-qty-value {
    color: white;
}

.character-overlay .additional-service-qty-btn {
    border-color: rgba(255, 255, 255, 0.6);
    background: rgba(255, 255, 255, 0.15);
    color: white;
}

.character-overlay .additional-service-qty-btn:hover {
    background: rgba(255, 255, 255, 0.25);
    border-color: white;
}

.additional-service-badge {
    position: absolute;
    top: 16px;
    right: 16px;
    z-index: 1;
    background: rgba(255, 255, 255,
----------------------------------------
Occurrence 12:
er-color: rgba(255, 255, 255, 0.6);
    background: rgba(255, 255, 255, 0.15);
    color: white;
}

.character-overlay .additional-service-qty-btn:hover {
    background: rgba(255, 255, 255, 0.25);
    border-color: white;
}

.additional-service-badge {
    position: absolute;
    top: 16px;
    right: 16px;
    z-index: 1;
    background: rgba(255, 255, 255, 0.18);
    color: white;
    border: 1px solid rgba(255, 255, 255, 0.28);
    backdrop-filter: blur(10px);
    padding: 0.35rem 0.8rem;
    border-radius: 9
----------------------------------------

==================== Selector: \.btn-select\b (Found 14) ====================
Occurrence 1:
sform: translateY(-3px);
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
}

/* Selection Button */
.btn-select {
    background: linear-gradient(45deg, var(--primary-color), var(--accent-color), var(--secondary-color));
    background-size: 400% 400%;
    color: white;
    animation: shimmer 3s ease-in-out infinite;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.8rem 1.8rem;
    font-weight: 600;
    box-shadow: 0 5px 20px rgba(230, 57, 70, 0.3);
}

.btn-select:hover {

----------------------------------------
Occurrence 2:
padding: 0.8rem 1.8rem;
    font-weight: 600;
    box-shadow: 0 5px 20px rgba(230, 57, 70, 0.3);
}

.btn-select:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 30px rgba(230, 57, 70, 0.5);
}

.btn-select-checkbox {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 20px;
    height: 20px;
    border: 2px solid white;
    border-radius: 4px;
    background: transparent;
    font-size: 14px;
    opacity: 0;
    transition: all 0.3s ease;
}

.btn-select.s
----------------------------------------
Occurrence 3:
lect:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 30px rgba(230, 57, 70, 0.5);
}

.btn-select-checkbox {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 20px;
    height: 20px;
    border: 2px solid white;
    border-radius: 4px;
    background: transparent;
    font-size: 14px;
    opacity: 0;
    transition: all 0.3s ease;
}

.btn-select.selected .btn-select-checkbox {
    opacity: 1;
    background: white;
    color: var(--primary-color);
}

.
----------------------------------------
Occurrence 4:
    background: transparent;
    font-size: 14px;
    opacity: 0;
    transition: all 0.3s ease;
}

.btn-select.selected .btn-select-checkbox {
    opacity: 1;
    background: white;
    color: var(--primary-color);
}

.btn-select.selected {
    background: linear-gradient(45deg, #ff9500, #ff6b35);
    background-size: 100% 100%;
    animation: none;
}

.btn-select-inline {
    font-size: 0.9rem;
    padding: 0.6rem 1.2rem;
}

[data-character-id].character-card.selected,
[data-character-id].character-card.
----------------------------------------
Occurrence 5:
parent;
    font-size: 14px;
    opacity: 0;
    transition: all 0.3s ease;
}

.btn-select.selected .btn-select-checkbox {
    opacity: 1;
    background: white;
    color: var(--primary-color);
}

.btn-select.selected {
    background: linear-gradient(45deg, #ff9500, #ff6b35);
    background-size: 100% 100%;
    animation: none;
}

.btn-select-inline {
    font-size: 0.9rem;
    padding: 0.6rem 1.2rem;
}

[data-character-id].character-card.selected,
[data-character-id].character-card.character-card-select
----------------------------------------
Occurrence 6:
d .btn-select-checkbox {
    opacity: 1;
    background: white;
    color: var(--primary-color);
}

.btn-select.selected {
    background: linear-gradient(45deg, #ff9500, #ff6b35);
    background-size: 100% 100%;
    animation: none;
}

.btn-select-inline {
    font-size: 0.9rem;
    padding: 0.6rem 1.2rem;
}

[data-character-id].character-card.selected,
[data-character-id].character-card.character-card-selected {
    box-shadow: 0 10px 40px rgba(255, 149, 0, 0.4);
    border: 2px solid #ff9500;
}

.btn-ou
----------------------------------------
Occurrence 7:
: linear-gradient(45deg, #ff9500, #ff6b35);
    background-size: 100% 100%;
    animation: none;
}

.btn-select-inline {
    font-size: 0.9rem;
    padding: 0.6rem 1.2rem;
}

[data-character-id].character-card.selected,
[data-character-id].character-card.character-card-selected {
    box-shadow: 0 10px 40px rgba(255, 149, 0, 0.4);
    border: 2px solid #ff9500;
}

.btn-outline {
    background: transparent;
    color: var(--primary-color);
    border: 2px solid var(--primary-color);
}

.btn-outline:hover {
----------------------------------------
Occurrence 8:
all 0.3s ease;
}

/* Небольшое увеличение при наведении */
.character-card:hover .character-overlay .btn-select {
    transform: scale(1.05);
}

.program-card:hover .character-overlay .btn-select {
    transform: none;
}

.character-info {
    padding: 1.5rem;
}

.character-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    margin-bottom: 0.5rem;
}

.btn-order-inline {
    opacity: 0;
    transform: translateX(20px);
    transition: all 0.3s ease;
  
----------------------------------------
Occurrence 9:
aracter-overlay .btn-select {
    transform: scale(1.05);
}

.program-card:hover .character-overlay .btn-select {
    transform: none;
}

.character-info {
    padding: 1.5rem;
}

.character-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    margin-bottom: 0.5rem;
}

.btn-order-inline {
    opacity: 0;
    transform: translateX(20px);
    transition: all 0.3s ease;
    pointer-events: none;
    padding: 0.5rem 1rem;
    font-size: 0.9rem;
    white-s
----------------------------------------
Occurrence 10:
portant;
        right: 6px !important;
        left: 6px !important;
    }

    .character-overlay .btn-select {
        width: 100% !important;
        padding: 0.35rem 0.5rem !important;
        font-size: 0.75rem !important;
        justify-content: center !important;
        border-radius: 10px !important;
        min-height: auto !important;
        height: auto !important;
    }

    .character-overlay .btn-select-hours {
        width: 100% !important;
        padding: 0.25rem 0.5rem !important;
  
----------------------------------------
Occurrence 11:
        min-height: auto !important;
        height: auto !important;
    }

    .character-overlay .btn-select-hours {
        width: 100% !important;
        padding: 0.25rem 0.5rem !important;
        font-size: 0.75rem !important;
        justify-content: space-between !important;
        border-radius: 10px !important;
        height: auto !important;
        min-height: auto !important;
    }

    .character-overlay .btn-select-hours button {
        width: 20px !important;
        height: 20px !impo
----------------------------------------
Occurrence 12:
        height: auto !important;
        min-height: auto !important;
    }

    .character-overlay .btn-select-hours button {
        width: 20px !important;
        height: 20px !important;
        font-size: 0.8rem !important;
        line-height: 18px !important;
    }

    .character-overlay .hours-value {
        font-size: 0.75rem !important;
    }

    /* Image Badges and counters */
    .character-photo-counter {
        top: 6px !important;
        right: 6px !important;
        padding: 0.15rem 
----------------------------------------
Occurrence 13:
background: rgba(46, 213, 115, 0.1);
    border-radius: 10px;
}

/* Hours selector inside button */
.btn-select-hours {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    padding: 0.4rem 0.5rem;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.18);
    backdrop-filter: blur(8px);
}

.hours-value {
    font-size: 1rem;
    font-weight: 700;
    color: white;
    min-width: 60px;
    text-align: center;
}

.hours-minus,
.hours-plus {
    display: inline-flex;
    align-ite
----------------------------------------
Occurrence 14:
imary-color);
}

.additional-service-action {
    white-space: nowrap;
}

.additional-service-action.btn-select {
    min-width: 142px;
    justify-content: center;
}

.additional-service-qty {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    padding: 0.4rem 0.5rem;
    border-radius: 999px;
    background: rgba(230, 57, 70, 0.08);
    backdrop-filter: blur(8px);
}

.additional-service-qty-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width
----------------------------------------

==================== Selector: \.character-image-container (Found 2) ====================
Occurrence 1:
s ease-out;
}

/* Увеличение изображения управляется через JavaScript для следования за курсором */
.character-image-container {
    cursor: pointer;
}

/* Убираем старый эффект */

.character-emoji {
    font-size: 8rem;
    filter: drop-shadow(0 5px 15px rgba(0, 0, 0, 0.2));
    animation: bounce 2s infinite;
    display: flex;
    align-items: center;
    justify-content: center;
}

@keyframes bounce {
    0%, 100% {
        transform: translateY(0);
    }
    50% {
        transform: translateY(-10px);
    }
}

.char
----------------------------------------
Occurrence 2:
32, 1.275);
    animation: fadeInUp 0.6s ease backwards;
}

/* Убираем дублирующий hover эффект */

.character-image-container {
    position: relative;
    overflow: hidden;
}

.character-badges {
    position: absolute;
    top: 1rem;
    left: 1rem;
    z-index: 10;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
}

/* Счётчик фотографий на карточке персонажа */
.character-photo-counter {
    position: absolute;
    bottom: 0.75rem;
    left: 0.75rem;
    z-index: 10;
    display: inline-flex;
    alig
----------------------------------------
